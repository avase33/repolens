"""RepoLens — X-ray vision for any Git repository.

RepoLens reads a repository's full Git history and surfaces the things that
matter but are normally invisible: code hotspots, knowledge risk (bus factor),
hidden temporal coupling between files, and how activity has evolved over time.

It runs fully offline with no API keys required. An optional AI narrative layer
turns the numbers into a plain-English briefing when an API key is present.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
